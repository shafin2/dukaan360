<!-- Stats Cards -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-6">
  <!-- Total Products -->
  <div class="bg-white overflow-hidden shadow-sm rounded-lg border border-gray-200">
    <div class="p-6">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center">
            <i data-feather="package" class="w-6 h-6 text-blue-600"></i>
          </div>
        </div>
        <div class="<%= I18n.locale == :ur ? 'mr-4' : 'ml-4' %>">
          <p class="text-sm font-medium text-gray-600"><%= t('products.stats.total_products') %></p>
          <p class="text-2xl font-bold text-gray-900"><%= current_user.shop.products.count %></p>
        </div>
      </div>
    </div>
  </div>

  <!-- Low Stock -->
  <div class="bg-white overflow-hidden shadow-sm rounded-lg border border-gray-200">
    <div class="p-6">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <div class="w-12 h-12 bg-orange-100 rounded-lg flex items-center justify-center">
            <i data-feather="alert-triangle" class="w-6 h-6 text-orange-600"></i>
          </div>
        </div>
        <div class="<%= I18n.locale == :ur ? 'mr-4' : 'ml-4' %>">
          <p class="text-sm font-medium text-gray-600"><%= t('products.stats.low_stock') %></p>
          <p class="text-2xl font-bold text-gray-900"><%= current_user.shop.products.low_stock.count %></p>
        </div>
      </div>
    </div>
  </div>

  <!-- Out of Stock -->
  <div class="bg-white overflow-hidden shadow-sm rounded-lg border border-gray-200">
    <div class="p-6">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <div class="w-12 h-12 bg-red-100 rounded-lg flex items-center justify-center">
            <i data-feather="x-circle" class="w-6 h-6 text-red-600"></i>
          </div>
        </div>
        <div class="<%= I18n.locale == :ur ? 'mr-4' : 'ml-4' %>">
          <p class="text-sm font-medium text-gray-600"><%= t('products.stats.out_of_stock') %></p>
          <p class="text-2xl font-bold text-gray-900"><%= current_user.shop.products.out_of_stock.count %></p>
        </div>
      </div>
    </div>
  </div>

  <!-- Expiring Soon -->
  <div class="bg-white overflow-hidden shadow-sm rounded-lg border border-gray-200">
    <div class="p-6">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <div class="w-12 h-12 bg-yellow-100 rounded-lg flex items-center justify-center">
            <i data-feather="clock" class="w-6 h-6 text-yellow-600"></i>
          </div>
        </div>
        <div class="<%= I18n.locale == :ur ? 'mr-4' : 'ml-4' %>">
          <p class="text-sm font-medium text-gray-600"><%= t('products.stats.expiring_soon') %></p>
          <p class="text-2xl font-bold text-gray-900"><%= current_user.shop.products.expiring_soon.count %></p>
        </div>
      </div>
    </div>
  </div>
</div>

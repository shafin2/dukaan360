<!DOCTYPE html>
<html lang="<%= I18n.locale %>" dir="<%= I18n.locale == :ur ? 'rtl' : 'ltr' %>">
  <head>
    <title>Dukaan360 - <%= t('auth.sign_in') %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application.tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <!-- Custom Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Amiri:wght@400;700&family=Cairo:wght@300;400;600;700&display=swap" rel="stylesheet">
  </head>

  <body class="min-h-screen bg-gray-50 flex flex-col justify-center py-12 sm:px-6 lg:px-8 <%= I18n.locale == :ur ? 'font-urdu' : 'font-sans' %>">
    <div class="sm:mx-auto sm:w-full sm:max-w-md">
      <div class="flex justify-center">
        <div class="w-16 h-16 bg-blue-600 rounded-xl flex items-center justify-center">
          <span class="text-white font-bold text-2xl">D</span>
        </div>
      </div>
      <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
        Dukaan360
      </h2>
      <p class="mt-2 text-center text-sm text-gray-600">
        <%= t('auth.shop_management_system') %>
      </p>
    </div>

    <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
      <div class="bg-white py-8 px-4 shadow-lg sm:rounded-lg sm:px-10">
        <%= yield %>
      </div>
      
      <!-- Language Toggle -->
      <div class="mt-6 text-center">
        <%= link_to "?locale=#{I18n.locale == :en ? 'ur' : 'en'}", 
            class: "inline-flex items-center px-4 py-2 text-sm text-gray-500 hover:text-gray-700 transition-colors" do %>
          <%= I18n.locale == :en ? 'اردو میں دیکھیں' : 'View in English' %>
        <% end %>
      </div>
    </div>
  </body>
</html>
